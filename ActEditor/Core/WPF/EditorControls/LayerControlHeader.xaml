<UserControl x:Class="ActEditor.Core.WPF.EditorControls.LayerControlHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Core="clr-namespace:ActEditor.Core"
             mc:Ignorable="d" >
	<UniformGrid Background="{DynamicResource UIGridBackgroundLightBrush}" Name="_grid" Columns="9" >
		<UniformGrid.Resources>
			<Style TargetType="Border">
				<Setter Property="BorderBrush" Value="{DynamicResource UIGridBackgroundBrush}" />
				<Setter Property="BorderThickness" Value="0,0,1,1" />
			</Style>
			<Style TargetType="TextBlock">
				<Setter Property="HorizontalAlignment" Value="Center" />
				<Setter Property="VerticalAlignment" Value="Center" />
				<Setter Property="Padding" Value="0" />
			</Style>
		</UniformGrid.Resources>
		<Border x:Name="_tbLayerId">
			<TextBlock Grid.Column="0" Text="Id" />
		</Border>
		<Border x:Name="_tbSpriteId">
			<TextBlock Text="Sprite" />
		</Border>
		<Border>
			<TextBlock Text="X" />
		</Border>
		<Border>
			<TextBlock Text="Y" />
		</Border>
		<Border>
			<TextBlock Text="Mirror" />
		</Border>
		<Border>
			<TextBlock Text="Color" />
		</Border>
		<Border>
			<TextBlock Text="Scale X" FontSize="12" />
		</Border>
		<Border>
			<TextBlock Text="Scale Y" FontSize="12" />
		</Border>
		<Border>
			<TextBlock Text="Angle" />
		</Border>
    </UniformGrid>
</UserControl>
